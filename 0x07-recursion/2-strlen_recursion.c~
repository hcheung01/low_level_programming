#include "holberton.h"

/**
 * _strlen_recursion - function with one argument
 * @s: char type pointer argument
 *
 * Description: returns the length of a string
 * Return: pointer s
 */
int _strlen_recursion(char *s)
{
	if (!*s)
		return 0;
	else
	{
		s++;
		return 1 + _str_recursion(s);
	}
}
